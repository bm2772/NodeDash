// Deployment config, loaded before the app.
// For a hosted frontend (e.g. Vercel), set this to your always-on backend URL:
//   window.__ND_API_BASE__ = "https://nodedash-api.example.com";
// Leave empty for local dev (defaults to http://<host>:8000). The start screen's
// "Backend URL" field still overrides this at runtime (stored in localStorage).
window.__ND_API_BASE__ = "";
